server:
  port: 16002
spring:
  application:
    name: stream-sink
  cloud:
    stream:
      bindings:
        output:
          destination: sink-channel
        input:
          destination: source-channel
management:
  security:
    enabled: false